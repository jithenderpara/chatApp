import { ChatboxDirective } from './chatbox.directive';

describe('ChatboxDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatboxDirective();
    expect(directive).toBeTruthy();
  });
});
